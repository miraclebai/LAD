sf: 4
model:
  base_learning_rate: 5.0e-05
  target: ldm.models.diffusion.ddpm.HazeLatentDiffusion
  params:
    ckpt_path: "/disk8t/baijy/Stable_main_2/logs/2025-12-12T23-17-28_stage2/checkpoints/epoch=000013-v2.ckpt"
    linear_start: 0.00085
    linear_end: 0.0120
    num_timesteps_cond: 1
    log_every_t: 200
    timesteps: 1000
    first_stage_key: image
    cond_stage_key: caption
    image_size: 512
    channels: 4
    cond_stage_trainable: False   # Note: different from the one we trained before
    conditioning_key: crossattn
    monitor: val/loss_simple
    scale_factor: 0.18215
    use_ema: False
    # for training only
    # ckpt_path:  /home/intern/ztw/Methods/LatentDehazing/logs/2024-03-25T16-04-40_v2-finetune_text_T_512/checkpoints/epoch=000067.ckpt
    unfrozen_diff: False
    random_size: False
    time_replace: 1000
    use_usm: True


    unet_config:
      target: ldm.modules.diffusionmodules.openaimodel.UNetModelDualcondV2
      params:
        image_size: 32 # unused
        in_channels: 4
        out_channels: 4
        model_channels: 320
        attention_resolutions: [ 4, 2, 1 ]
        num_res_blocks: 2
        channel_mult: [ 1, 2, 4, 4 ]
        num_head_channels: 64
        use_spatial_transformer: True
        use_linear_in_transformer: True
        transformer_depth: 1
        context_dim: 1024
        use_checkpoint: False
        legacy: False
        semb_channels: 256

    first_stage_config:
      target: ldm.models.autoencoder.HazeAutoencoderKLResi
      params:
        # for training only
        ckpt_path: "/disk8t/baijy/Stable_main_2/logs/2025-12-17T10-24-28_dec/checkpoints/last.ckpt"
        embed_dim: 4
        monitor: val/rec_loss
        ddconfig:
          double_z: true
          z_channels: 4
          resolution: 512
          in_channels: 3
          out_ch: 3
          ch: 128
          ch_mult:
          - 1
          - 2
          - 4
          - 4
          num_res_blocks: 2
          attn_resolutions: [128, 64]
          dropout: 0.0
        lossconfig:
          target: torch.nn.Identity


    cond_stage_config:
      target: ldm.modules.encoders.modules.FrozenOpenCLIPEmbedder
      params:
        freeze: True
        layer: "penultimate"

    structcond_stage_config:
      target: ldm.modules.diffusionmodules.openaimodel.EncoderUNetModelWT
      params:
        image_size: 64
        in_channels: 1
        model_channels: 256
        out_channels: 256
        num_res_blocks: 2
        attention_resolutions: [ 4, 2, 1 ]
        dropout: 0
        channel_mult: [ 1, 1, 2, 2 ]
        conv_resample: True
        dims: 2
        use_checkpoint: False
        use_fp16: False
        num_heads: 4
        num_head_channels: -1
        num_heads_upsample: -6
        use_scale_shift_norm: False
        resblock_updown: False
        use_new_attention_order: False
        





data:
  target: main.DataModuleFromConfig
  params:
    batch_size: 1
    num_workers: 6
    wrap: false
    train:
      target: basicsr.data.reside_dataset.RESIDEDataset
      params:
        phase : train
        gt_path: '/home/intern/ztw/Data/RESIDE_new/HR'
        dataroot_depth: '/home/intern/ztw/Data/RESIDE_new/HR_depth'
        ref_path: '/home/intern/ztw/Data/RESIDE_ours/HR_hazy_src'
        crop_size: 512
        io_backend:
          type: disk
        use_hflip: True
        use_rot: True
    validation:
      target: basicsr.data.paired_image_dataset.PairedImageDataset
      params:
        dataroot_gt: '/home/intern/ztw/Data/I-HAZE-PROCESS/HR'
        dataroot_lq: '/home/intern/ztw/Data/I-HAZE-PROCESS/HR_hazy'
        
        scale : 1
        phase : val
        io_backend:
          type: disk

        # blur_kernel_size: 21
        # kernel_list: ['iso', 'aniso', 'generalized_iso', 'generalized_aniso', 'plateau_iso', 'plateau_aniso']
        # kernel_prob: [0.45, 0.25, 0.12, 0.03, 0.12, 0.03]
        # sinc_prob: 0.1
        # blur_sigma: [0.2, 1.5]
        # betag_range: [0.5, 2.0]
        # betap_range: [1, 1.5]

        # blur_kernel_size2: 11
        # kernel_list2: ['iso', 'aniso', 'generalized_iso', 'generalized_aniso', 'plateau_iso', 'plateau_aniso']
        # kernel_prob2: [0.45, 0.25, 0.12, 0.03, 0.12, 0.03]
        # sinc_prob2: 0.1
        # blur_sigma2: [0.2, 1.0]
        # betag_range2: [0.5, 2.0]
        # betap_range2: [1, 1.5]

        # final_sinc_prob: 0.8

        gt_size: 512
        use_hflip: True
        use_rot: False

test_data:
  target: main.DataModuleFromConfig
  params:
    batch_size: 1
    num_workers: 6
    wrap: false
    test:
    # used to be basicsr.data.realesrgan_dataset.RealESRGANDataset
      target: basicsr.data.paired_image_dataset.PairedImageDataset
      params:
        dataroot_gt: '/home/intern/ztw/ztw/ztw/Methods/Difflare/data/Flare7Kpp/test_data/real/gt'
        dataroot_lq: '/home/intern/ztw/ztw/ztw/Methods/Difflare/data/Flare7Kpp/test_data/real/input'
        crop_size: 512
        scale : 1
        phase : train
        io_backend:
          type: disk

        # blur_kernel_size: 21
        # kernel_list: ['iso', 'aniso', 'generalized_iso', 'generalized_aniso', 'plateau_iso', 'plateau_aniso']
        # kernel_prob: [0.45, 0.25, 0.12, 0.03, 0.12, 0.03]
        # sinc_prob: 0.1
        # blur_sigma: [0.2, 1.5]
        # betag_range: [0.5, 2.0]
        # betap_range: [1, 1.5]

        # blur_kernel_size2: 11
        # kernel_list2: ['iso', 'aniso', 'generalized_iso', 'generalized_aniso', 'plateau_iso', 'plateau_aniso']
        # kernel_prob2: [0.45, 0.25, 0.12, 0.03, 0.12, 0.03]
        # sinc_prob2: 0.1
        # blur_sigma2: [0.2, 1.0]
        # betag_range2: [0.5, 2.0]
        # betap_range2: [1, 1.5]

        # final_sinc_prob: 0.8

        # gt_size: 512
        use_hflip: False
        use_rot: False

lightning:
  modelcheckpoint:
    params:
      every_n_train_steps: 200
  callbacks:
    image_logger:
      target: main.ImageLogger
      params:
        batch_frequency: 1000
        max_images: 1
        increase_log_steps: False

  trainer:
    benchmark: True
    max_steps: 800000
    accumulate_grad_batches: 8
